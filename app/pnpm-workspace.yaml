packages:
  - web
  - api
# pnpm blocks dependency build scripts by default; these need theirs
# (native builds / Prisma engine downloads).
allowBuilds:
  '@prisma/engines': true
  argon2: true
  esbuild: true
  prisma: true
